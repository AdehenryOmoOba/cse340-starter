<div class="feedback-container">
    <div class="feedback-card">
        <h1>Send Us Your Feedback</h1>
        <form action="/messages" method="POST" class="feedback-form">
            <div class="form-group">
                <textarea id="message_text" name="message_text" rows="5" required minlength="5" class="form-control"
                    placeholder=" "></textarea>
                <label for="message_text" class="form-label">Your Message</label>
            </div>
            <button type="submit" class="btn-primary">Send Feedback</button>
        </form>
    </div>
</div>
<style>
    .feedback-container {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 60vh;
        background: linear-gradient(120deg, #f8fafc 0%, #e0e7ef 100%);
    }

    .feedback-card {
        background: #fff;
        border-radius: 16px;
        box-shadow: 0 4px 24px rgba(0, 0, 0, 0.08);
        padding: 2.5rem 2rem 2rem 2rem;
        max-width: 420px;
        width: 100%;
    }

    .feedback-card h1 {
        font-size: 1.6rem;
        margin-bottom: 1.5rem;
        color: #2d3748;
        text-align: center;
    }

    .feedback-form .form-group {
        position: relative;
        margin-bottom: 1.5rem;
    }

    .feedback-form .form-control {
        width: 100%;
        padding: 1.2rem 1rem 0.6rem 1rem;
        border: 1px solid #cbd5e1;
        border-radius: 8px;
        background: #f9fafb;
        font-size: 1rem;
        resize: vertical;
        transition: border 0.2s;
    }

    .feedback-form .form-control:focus {
        border-color: #6366f1;
        outline: none;
        background: #fff;
    }

    .feedback-form .form-label {
        position: absolute;
        left: 1rem;
        top: 1.1rem;
        color: #64748b;
        font-size: 1rem;
        pointer-events: none;
        background: transparent;
        transition: 0.2s;
    }

    .feedback-form .form-control:focus+.form-label,
    .feedback-form .form-control:not(:placeholder-shown)+.form-label {
        top: -0.7rem;
        left: 0.8rem;
        font-size: 0.85rem;
        color: #6366f1;
        background: #fff;
        padding: 0 0.2rem;
    }

    .btn-primary {
        width: 100%;
        padding: 0.8rem;
        background: linear-gradient(90deg, #6366f1 0%, #60a5fa 100%);
        color: #fff;
        border: none;
        border-radius: 8px;
        font-size: 1.1rem;
        font-weight: 600;
        cursor: pointer;
        transition: background 0.2s;
    }

    .btn-primary:hover {
        background: linear-gradient(90deg, #4f46e5 0%, #2563eb 100%);
    }
</style>